<project xmlns="http://maven.apache.org/POM/4.0.0" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>br.com.nasajon</groupId>
    <artifactId>nsjAPI</artifactId>
    <packaging>pom</packaging>
    <version>${env.VERINFO}</version>
    <name>nsjAPI</name>

    <properties>
        <bplUtilsVersion>RELEASE</bplUtilsVersion>
        <bplLibrariesVersion>RELEASE</bplLibrariesVersion>
    </properties>

    <dependencies>
        <dependency>
            <groupId>br.com.nasajon</groupId>
            <artifactId>Nasajon.Utils</artifactId>
            <version>${bplUtilsVersion}</version>
            <type>zip</type>
        </dependency>
        <dependency>
            <groupId>br.com.nasajon</groupId>
            <artifactId>Nasajon.Libraries</artifactId>
            <version>${bplLibrariesVersion}</version>
            <type>zip</type>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>nasajon</id>
            <name>nasajon</name>
            <url>https://nexus.nasajon.com.br/repository/erp/</url>
        </repository>
    </repositories>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.1.1</version>
                <executions>
                    <execution>
                        <id>unzip-dependencies</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>unpack-dependencies</goal>
                        </goals>
                        <configuration>
                            <includeGroupIds>br.com.nasajon</includeGroupIds>
                            <includeArtifactIds>Nasajon.Utils,Nasajon.Libraries</includeArtifactIds>
                            <outputDirectory>${env.NSDCU}</outputDirectory>
                            <overWriteReleases>true</overWriteReleases>
                            <overWriteSnapshots>true</overWriteSnapshots>
                            <overWriteIfNewer>true</overWriteIfNewer>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.3</version>
                <executions>
                    <execution>
                        <phase>compile</phase>
                        <configuration>
                            <tasks>
                                <exec dir="${project.basedir}" executable="build.bat" failonerror="true">
                                </exec>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>